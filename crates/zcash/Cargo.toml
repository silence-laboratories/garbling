[package]
name = "zcash"
version = "0.1.0"
edition = "2024"

[dependencies]
garbled-circuit = { workspace = true }
sl-compute-common = { workspace = true }
sha2 = { workspace = true }
hmac = { workspace = true }
sl-messages = { workspace = true }
group = { workspace = true }
hex = "0.4"

[dev-dependencies]
criterion = "0.5.1"
rand = { workspace = true }
rand_chacha = "0.3"
pasta_curves = "0.5.1"
hex = "0.4"
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
multi-party-schnorr = {version = "1.2.0-pre.1", features = ["test-support", "redpallas"]}

[[bench]]
name = "blake2b_bench"
harness = false

[features]
test-support = []